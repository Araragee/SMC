# System Scan Report — 2026-05-30

Sweep of the SMC `dev` working tree. Code quality is solid: no bare `except:` in backend routers, **no** `console.log` in frontend source, no hardcoded `localhost`/URLs in the frontend, no `PlaceholderView` links, no `TODO/FIXME` in routers, and `.gitignore` correctly lists `*.db`, `.DS_Store`, `__pycache__`. The shop fulfillment flow even guards against overselling with an atomic stock update. Items below are the remaining opportunities, ordered by value-to-effort.

## High value / low effort

1. **Stale git lock blocking operations.** `.git/index.lock` (0 bytes, dated May 23) is present and caused `git stash`/`git checkout` to fail this run. Remove it: `rm "<repo>/.git/index.lock"`. Until then, automated git steps in scheduled tasks will keep failing.

2. **Uncommitted-change sprawl on `dev`.** The tree carries dozens of modified files plus many untracked files, including ~15 prior `session-summary-*` and `improvement-scan-*` markdown reports from May 22–24 sitting in the repo root. Recommend: commit the real work, and move (or `.gitignore`) the generated session/scan reports into a `docs/reports/` folder so the root stays clean and `git status` is meaningful.

3. **`.DS_Store` still tracked.** `.gitignore` lists `.DS_Store`, but at least one `.DS_Store` is still tracked in git. `git rm --cached` it.

## Medium value

4. **Order-status vocabulary mismatch (frontend ↔ backend).** `stores/shop.ts` gates a refetch on `['approved', 'cancelled', 'fulfilled']`, and the backend transition map allows `approved → fulfilled`. Confirm the `OrderStatus` type, the `OrderStatusBadge.vue` component, and the admin action buttons all share exactly the same status set (`pending/approved/fulfilled/rejected/cancelled`) so no state is unreachable from the UI.

5. **Debug `print()` in backend routers.** Two `print(` calls remain in `backend/routers/*.py`. Replace with the standard `logging` module (or remove) so production output stays clean.

6. **Test coverage for the fulfillment state machine.** `backend/test_main.py` exists (219 lines). Add cases asserting: illegal status transitions return `400`; the atomic stock guard returns `409` on a sold-out race; and stock is restored exactly once on `approved → cancelled`.

## Lower priority

7. **Payment receipts.** If printable/exportable receipts aren't yet covered by `routers/payments.py`, a print-friendly receipt view or a PDF endpoint would round out the payments module.

8. **Customer-facing fulfillment timeline.** The order lifecycle is rich on the backend; surfacing a simple status timeline in the student/teacher `Shop.vue` would cut "where's my order?" questions.

---

*Generated by the scheduled SMC maintenance task. No code for these items was written this run — this is a backlog for the next pass.*

node_modules/
dist/
.DS_Store
frontend/node_modules/
__pycache__
*.pyc
*.db
.claude/settings.local.json
.env
.env.local
backend/.env
# NOTE: backend/.env is still tracked in git from an old commit.
# Run: git rm --cached backend/.env && git commit -m "untrack .env"
# to fully remove it from git history going forward.
